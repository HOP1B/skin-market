{
  /* <div className="flex justify-between pr-4 pt-4 bg-[#1d1f20]">
          <div className="pl-5">
            <form className="relative w-[253px] h-12 mb-4">
              <ul className="mt-3 space-y-2">
                <input
                  type="text"
                  placeholder="Search"
                  value={query}
                  onChange={(e) => setQuery(e.target.value)}
                  className="w-full p-2 border rounded-md focus:outline-none focus:ring-2 focus:ring-[#2a2c2e] pl-12"
                />
              </ul>
              <div className="w-12 h-full flex items-center rounded justify-center absolute top-0 left-0 bg-[#2a2c2e]">
                <Search color="white" size={24} />
              </div>
            </form>
            <div className="grid grid-cols-11 gap-5">
              {filteredSkins.map((listing) => (
                <SkinCard
                  key={listing.id}
                  image={listing.skin.imageUrl}
                  price={listing.price}
                  condition={listing.status}
                  name={listing.skin.skinname}
                />
              ))}
            </div>
          </div>
          <ItemsFilter />
        </div> */
}
